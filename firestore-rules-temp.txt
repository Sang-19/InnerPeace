// TEMPORARY FIRESTORE RULES FOR TESTING
// Copy these rules to your Firebase Console -> Firestore Database -> Rules
// WARNING: These rules are NOT secure and should ONLY be used for development/testing

rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all operations for testing
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

// After testing works, replace with these secure rules:

/*
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Users can read/write their own data
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
      
      match /mood-logs/{moodId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      
      match /journalEntries/{entryId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
    }
    
    // Allow reading all users for admin functionality
    match /users/{userId} {
      allow read: if request.auth != null;
    }
    
    // Emergency alerts - any authenticated user can create
    match /alerts/{alertId} {
      allow create: if request.auth != null;
      allow read, update: if request.auth != null;
    }
    
    // Notifications
    match /notifications/{notificationId} {
      allow read, write: if request.auth != null;
    }
    
    // Community features
    match /community-chat/{messageId} {
      allow read, write: if request.auth != null;
    }
    
    match /community-reports/{reportId} {
      allow create, read: if request.auth != null;
    }
    
    // Test collection
    match /connection-test/{testId} {
      allow create, read: if request.auth != null;
    }
  }
}
*/
